---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="lg:grid lg:grid-cols-2 lg:items-start">
		<header class="flex flex-col justify-center lg:sticky lg:top-0">
			<slot name="header" />
		</header>
		<main class="flex flex-col justify-center">
			<slot />
		</main>
		<footer>
			<slot name="footer" />
		</footer>
	</body>
</html>
<style is:global>
	@font-face {
		font-family: "Anton";
		src: url("/fonts/Anton.ttf");
	}
	@font-face {
		font-family: "Antonio";
		src: url("/fonts/Antonio.ttf");
	}
	html {
		font-family: system-ui, sans-serif;
		color-scheme: dark light;
		@apply dark:text-slate-200 text-slate-800 dark:bg-stone-800 bg-slate-50 leading-relaxed;
		scroll-behavior: smooth;
	}
	.accent {
		@apply font-semibold dark:text-white text-black;
	}
	a {
		@apply hover:text-accent focus:text-accent active:text-accent dark:text-white text-black transition-colors w-fit;
	}
	h1,
	h2,
	h3,
	h4,
	h5,
	h6 {
		@apply dark:text-white text-slate-900;
	}

	h1,
	h2,
	h3 {
		font-family: "Anton", system-ui, sans-serif;
		font-weight: 500;
	}
</style>
