---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Anton&family=Antonio:wght@100..700&display=swap"
			rel="stylesheet"
		/>
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<header class="flex flex-col justify-center">
			<slot name="header" />
		</header>
		<main class="flex flex-col justify-center">
			<slot />
		</main>
		<footer>
			<slot name="footer" />
		</footer>
	</body>
</html>
<style is:global>
	html {
		font-family: system-ui, sans-serif;
		color-scheme: dark light;
		@apply dark:text-slate-200 text-slate-800 dark:bg-stone-800 bg-slate-50 leading-relaxed;
		scroll-behavior: smooth;
	}
	.accent {
		@apply font-semibold dark:text-white text-black;
	}
	a {
		@apply hover:text-accent hover:border-accent dark:text-white text-black border-black dark:border-white transition-colors w-fit inline-block;
	}
	h1,
	h2,
	h3,
	h4,
	h5,
	h6 {
		@apply dark:text-white text-slate-900;
	}

	h1,
	h2,
	h3 {
		font-family: "Anton";
	}
</style>
